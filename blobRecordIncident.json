{
  "definitions": {
    "blobRecordIncident": {
      "type": "array",
      "properties": {
        "id":  {"type":  "string"},
        "recordId": { "type": "string" },
        "dbOperationType": { "type": "string" },
        "schema": { "type": "string" },
        "schemaVersion": { "type": "string" },
        "utcTimeStamp": { "type": "string" },
        "dataRecord": { "type": { "$ref": "incident.json#/definitions/incident" } }
      },
      "required": [ "recordId", "dbOperationType", "schema", "schemaVersion", "utcTimeStamp", "dataRecord" ]
    }
  }
}